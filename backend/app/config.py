import os
from dotenv import load_dotenv
from pydantic_settings import BaseSettings

load_dotenv()

class Settings(BaseSettings):
    GROQ_API_KEY: str
    GROQ_MODEL: str = "llama-3.3-70b-versatile"
    TEMPERATURE: float = 0.1
    MAX_TOKENS: int = 4096
    
    class Config:
        env_file = ".env"

settings = Settings()

# Промпт для анализа технологических операций
TECH_PROCESS_PROMPT = """
Ты — инженер-технолог по механической обработке. Твоя задача:

1. ПРОАНАЛИЗИРОВАТЬ входной текст пользователя (свободная форма описания операций)
2. ПОДОБРАТЬ инструмент из базы знаний по параметрам:
   - Тип операции (точение, сверление, фрезерование, расточка и т.д.)
   - Материал заготовки
   - Размеры и допуски
   - Шероховатость поверхности
3. ПЕРЕФРАЗИРОВАТЬ описание операций в соответствии с ЕСКД (ГОСТ 3.1118, 3.1105):
   - Использовать стандартные формулировки
   - Указывать инструмент по ГОСТ/ТУ
   - Соблюдать порядок: операция → переход → инструмент → режимы
4. СФОРМИРОВАТЬ структурированный JSON для экспорта в Excel

Формат ответа — ТОЛЬКО JSON:
{
  "part_name": "Название детали",
  "material": "Материал заготовки",
  "operations": [
    {
      "op_number": "005",
      "op_name": "Токарная",
      "equipment": "Станок токарный 16К20",
      "transitions": [
        {
          "transition_number": 1,
          "description": "По ЕСКД: подрезать торец, выдержать размер...",
          "tool": {
            "name": "Резец токарный проходной",
            "gost": "ГОСТ 18871-73",
            "material": "Т15К6",
            "geometry": "80° ромбический",
            "size": "25x25x150"
          },
          "cutting_params": {
            "v_m_min": 120,
            "s_mm_rev": 0.3,
            "t_mm": 2.0
          }
        }
      ]
    }
  ]
}

База инструментов и правила ЕСКД будут предоставлены отдельно.
Не придумывай параметры — используй только из базы или стандартные значения.
"""